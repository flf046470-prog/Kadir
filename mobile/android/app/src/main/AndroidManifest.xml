<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Android App Links.

                A referral link is the whole point of this: someone taps
                fiorematch.com/tr/register?ref=ABCD2345 in a message and lands
                in the app with the code already filled in, rather than in a
                browser that then has to hand them off.

                `autoVerify` is what makes Android open the app WITHOUT the
                "open with" chooser, and it only works once
                /.well-known/assetlinks.json on the domain lists this app's
                package and its signing-certificate fingerprint. That file
                cannot be written until the release keystore exists — see
                docs/mobile.md. Until then the link still opens the app, just
                via the chooser.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="fiorematch.com" />
                <data android:scheme="https" android:host="www.fiorematch.com" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Android 13+ requires the app to ask before it may show a notification.
        Declaring it is not the same as being granted it: the request is made
        from the app, after the member has a reason to say yes, rather than on
        first launch when they have none.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
